.DS_Store
__pycache__/
*.py[cod]

# Local machine config — not part of the site
.claude/

# Local preview scaffolding, served from the repo root during checks
_preview*.html
_p.jpg
_ruud-cropped.jpg

# Built self-hosted site — regenerate with: cd builder && python3 build_site.py
site/

# npm — the built bundle and the lockfile are committed, the tree that produced
# them is not
node_modules/
