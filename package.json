{
  "name": "company-website",
  "version": "1.0.0",
  "description": "Everything for extremeheating.com lives here: the images the live site loads, the generator that builds its pages, and the Framer components. One repo, one place to look.",
  "main": "index.js",
  "scripts": {
    "build:schedule": "esbuild src/schedule/mount.tsx --bundle --minify --format=iife --target=es2018 --jsx=automatic --define:process.env.NODE_ENV=\\\"production\\\" --outfile=assets/js/schedule.js"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/extremheating-cloud/company-website.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "bugs": {
    "url": "https://github.com/extremheating-cloud/company-website/issues"
  },
  "homepage": "https://github.com/extremheating-cloud/company-website#readme",
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "esbuild": "^0.28.1"
  }
}
